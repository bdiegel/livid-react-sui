import { StyleSheet } from 'aphrodite';

export const styles = StyleSheet.create({
    alignDate: {
        float: 'right', 
        position: 'relative', 
        top: '50%', 
        transform: 'translateY(50%)',
    }
});